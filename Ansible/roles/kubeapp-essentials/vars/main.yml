---
cloud_user: ""

domain_tld: ""

idam_server_ip: ""

idam_short_hostname: ""

kube_cluster_name: ""

kubeapp_dir: '/home/{{ cloud_user }}/kubeapps/{{ kube_cluster_name }}'

kubeconfig: '/home/{{ cloud_user }}/kubeconfig/kubeconfig-{{ kube_cluster_name }}.yaml'

certmanager_min_ver: v1.20

certmanager_namespace: cert-manager

tsig_secret_name: ipa-tsig

external_dns_min_ver: 1.21
