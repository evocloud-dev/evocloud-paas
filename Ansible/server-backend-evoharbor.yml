---
#------------------------------------------------------
# This role deploys EvoHarbor OCI Registry
#------------------------------------------------------
- hosts: all
  gather_facts: false

  vars:
    creds_store: "{{ lookup('file', ansible_secret) | from_yaml }}"

  roles:
    - app-dnsmasq #Needed by GCP and Hetzner Compute Instances
    - rocky8-hardening
    - config-freeipa-client
    - config-freeipa-certs
    - webapp-evoharbor
    - app-fail2ban