version: 3

silent: true

vars:
  # Common across tasks
  DEPLOY_DIR:          "/opt/EVOCLOUD/Terraform/azure/deployment"
  KEYS_DIR:            "/opt/EVOCLOUD/Keys"
  BUILD_CMD:           "terragrunt run --all --non-interactive --queue-include-external apply -- -auto-approve"
  CLUSTER_DESTROY_CMD: "terragrunt apply --non-interactive -auto-approve"
  DESTROY_CMD:         "terragrunt destroy --non-interactive -auto-approve"


tasks:
  # Internal tasks that remove redundant code and simplifies logic
  # Internal tasks do not show up available task list.
  # SERVER is the server being operated on.
  # ACTION is BUILD_CMD or DESTROY_CMD
  vm-operation:
    internal: true
    cmds:
      - |
        cd {{.DEPLOY_DIR}}/{{.SERVER}}/ && \
        {{.ACTION}}  

  ###############################
  # Neutral/Dependency Tasks
  ###############################
  copy-files:
    # Task only runs if one or more status commands not satisfied"
    # Files: gcp-evocloud.pem, gcp-evocloud.pub, ansible-vault-pass.txt, secret-store.yml and root.hcl
    desc: "HCLOUD: Copies files from mount directory inside container to their respective locations."
    status:
      - "ls -al Ansible/secret-vault | grep ansible-vault-pass.txt"
      - "ls -al Ansible/secret-vault | grep secret-store.yml"
    cmds:
      # The slash in front of the cp commands is required to prevent user from being prompted to confirm.
      # The cp command in rocky linux is aliased to cp -i.
      - \cp {{.MNTDIR}}/azure/*.pem /etc/pki/tls
      - \cp {{.MNTDIR}}/azure/*.pub /etc/pki/tls
      - \cp {{.MNTDIR}}/azure/ansible-vault-pass.txt {{.MNTDIR}}/azure/secret-store.yml Ansible/secret-vault
      - \cp {{.MNTDIR}}/azure/root.hcl {{.DEPLOY_DIR}}
      - chmod 0600 /etc/pki/tls/*.pem
      - chmod 0644 /etc/pki/tls/*.pub

  ###############################
  # Build Tasks
  ###############################
  build-evo-master:
    desc: "AZURE: Build evo-master Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building evo-master"
      - task: vm-operation
        vars:
          SERVER: server-01-dmz-deployer
          ACTION: "{{.BUILD_CMD}}"

  build-evo-idp:
    desc: "AZURE: Build EvoCloud IdM Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud IdM Server"
      - task: vm-operation
        vars:
          SERVER: server-02-admin-idam-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-idpr:
    desc: "AZURE: Build EvoCloud IdM Replica Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud IdM Replica Server"
      - task: vm-operation
        vars:
          SERVER: server-03-admin-idam_replica-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-rdp:
    desc: "AZURE: Build EvoCloud Remote Desktop Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud Remote Desktop Server"
      - task: vm-operation
        vars:
          SERVER: server-dmz-rdp-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-cluster-std:
    desc: "AZURE: Build EvoCloud Single Node Cluster"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud Single Node Kube Cluster"
      - task: vm-operation
        vars:
          SERVER: cluster-talos-standalone-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-cluster-mgr:
    desc: "AZURE: Build EvoCloud Full Kube Cluster"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building Full EvoCloud Kube Cluster"
      - task: vm-operation
        vars:
          SERVER: cluster-admin-talos-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-code:
    desc: "AZURE: Build EvoCloud Code Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud Code Server"
      - task: vm-operation
        vars:
          SERVER: server-backend-evocode-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-code-runner:
    desc: "AZURE: Build EvoCloud Code Runner Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud Code Runner Server"
      - task: vm-operation
        vars:
          SERVER: server-backend-evocode-runner-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-harbor:
    desc: "AZURE: Build EvoCloud Harbor Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud Harbor Server"
      - task: vm-operation
        vars:
          SERVER: server-backend-evoharbor-helper
          ACTION: "{{.BUILD_CMD}}"

  build-evo-vcd:
    desc: "AZURE: Build EvoCloud Virtual Cloud Desktop Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Building EvoCloud Virtual Cloud Desktop Server"
      - task: vm-operation
        vars:
          SERVER: server-dmz-vcd-helper
          ACTION: "{{.BUILD_CMD}}"

  ###############################
  # Destroy Tasks
  ###############################
  destroy-evo-master:
    desc: "AZURE: Destroy evo-master"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying evo-master"
      - task: vm-operation
        vars:
          SERVER: server-01-dmz-deployer
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-idp:
    desc: "AZURE: Destroy EvoCloud IdM Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying EvoCloud IdM Server"
      - task: vm-operation
        vars:
          SERVER: server-02-admin-idam
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-idpr:
    desc: "AZURE: Destroy EvoCloud IdM Replica Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying EvoCloud IdM Replica Server"
      - task: vm-operation
        vars:
          SERVER: server-03-admin-idam_replica
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-rdp:
    desc: "AZURE: Destroy EvoCloud Remote Desktop Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying EvoCloud Remote Desktop Server"
      - task: vm-operation
        vars:
          SERVER: server-dmz-rdp
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-cluster-std:
    desc: "AZURE: Destroy EvoCloud Single Node Kube Cluster"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying EvoCloud Single Node Kube Cluster"
      - task: vm-operation
        vars:
          SERVER: cluster-talos-standalone-helper-destroyer
          ACTION: "{{.CLUSTER_DESTROY_CMD}}"

  destroy-evo-cluster-mgr:
    desc: "AZURE: Destroy EvoCloud Full  Kube Cluster"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying EvoCloud Full Kube Cluster"
      - task: vm-operation
        vars:
          SERVER: cluster-admin-talos-helper-destroyer
          ACTION: "{{.CLUSTER_DESTROY_CMD}}"

  destroy-evo-code:
    desc: "AZURE: Destroy EvoCloud Code Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroy EvoCloud Code Server"
      - task: vm-operation
        vars:
          SERVER: server-backend-evocode
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-code-runner:
    desc: "AZURE: Destroy EvoCloud Code Runner Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroy EvoCloud Code Runner Server"
      - task: vm-operation
        vars:
          SERVER: server-backend-evocode-runner
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-harbor:
    desc: "AZURE: Destroy EvoCloud Harbor Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destory EvoCloud Harbor Server"
      - task: vm-operation
        vars:
          SERVER: server-backend-evoharbor
          ACTION: "{{.DESTROY_CMD}}"

  destroy-evo-vcd:
    desc: "AZURE: Destroy EvoCloud Virtual Cloud Desktop Server"
    deps: [copy-files]
    cmds:
      - echo "AZURE':' Destroying EvoCloud Virtual Cloud Desktop Server"
      - task: vm-operation
        vars:
          SERVER: server-dmz-vcd
          ACTION: "{{.DESTROY_CMD}}"